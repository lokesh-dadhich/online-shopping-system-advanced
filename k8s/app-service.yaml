apiVersion: v1
kind: Service
metadata:
  name: online-shopping-service
spec:
  type: NodePort
  selector:
    app: online-shopping-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
